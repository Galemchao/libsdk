apply plugin: 'com.android.library'

android {
    compileSdkVersion 25
    buildToolsVersion '26.0.2'
    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 23
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
            manifestPlaceholders = [
                AMAP_LOCKEY: "8871ca2a38b186457ddd2da05662972d",
            ]
        }
        debug {
            manifestPlaceholders = [
                AMAP_LOCKEY: "8871ca2a38b186457ddd2da05662972d",
            ]
        }
    }
    sourceSets {
        main {
            jni.srcDirs = []
            jniLibs.srcDirs = ['libs']
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile project(path: ':libcocos2dx')
    compile project(path: ':easypermissions')
    compile "com.android.support:appcompat-v7:25.+"
    compile "com.android.support:support-compat:25.+"
    compile "com.android.support:support-fragment:25.+"
}
